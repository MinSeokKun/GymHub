# Core DB 연결 정보 (메타 데이터 저장용)
DATABASE_URL_CORE="mysql://username:password@localhost:3306/core_db"

# 테넌트 DB 연결을 위한 기본 URL (DB 이름 제외)
DATABASE_URL_TENANT_BASE="mysql://username:password@localhost:3306"

# 새 테넌트 DB 생성을 위한 연결 정보
DB_TEMPLATE="mysql://username:password@localhost:3306"

# JWT 암호화 키 (인증용)
JWT_SECRET="your-secret-key-here"

# 환경 설정
NODE_ENV="development"